import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Final Accounts Builder — Journal to Balance Sheet",
  description: "Build a complete, formula-linked accounting workbook (Journal, Trial Balance, Adjustments, Trading A/c, P&L A/c, Balance Sheet, Notes) from your business transactions. Nepali/Indian accounting format.",
  keywords: ["accounting", "bookkeeping", "trial balance", "balance sheet", "profit and loss", "trading account", "audit report", "Nepali accounting", "Indian accounting", "Excel workbook"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Final Accounts Builder",
    description: "Turn transactions into a complete set of final accounts — fully formula-linked Excel workbook.",
    url: "https://chat.z.ai",
    siteName: "Z.ai",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Final Accounts Builder",
    description: "Turn transactions into a complete set of final accounts — fully formula-linked Excel workbook.",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
